package {{engine}}_api

import (
	"github.com/gin-gonic/gin"
)

// Routes registers the engine's HTTP endpoints. The group is already mounted
// at the engine's MountPath.
func Routes(r *gin.RouterGroup) {
	r.GET("/index", IndexAction)
}
