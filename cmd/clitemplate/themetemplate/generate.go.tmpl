package {{package}}

// Regenerate the Go code for this theme's .templ files.
//
//	$ go generate ./...
//
// The generated *_templ.go files are committed, so building and testing this
// module does not require the templ CLI.
//go:generate go tool templ generate -path .
