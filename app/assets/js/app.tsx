// Frontend bundle entry. The island runtime (data-island scanner and mount
// logic) lands in Phase 3; until then the bundle is intentionally empty so
// the build pipeline ships end to end.
export {}
